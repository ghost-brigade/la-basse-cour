const SendButton = (props) => {
    return (
        <button type='submit' className='app_button-icon'>
            <i className="fa fa-send"/>Envoyer
        </button>
    );
}

export default SendButton;